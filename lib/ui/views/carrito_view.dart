import 'package:flutter/material.dart';

class carrito_view extends StatelessWidget {
  const carrito_view({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(child: Text("carrito"));
  }
}
